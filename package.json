{
  "name": "dice-craft",
  "version": "0.1.0",
  "description": "基于多Agent的桌游创作与游玩平台",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test tests/",
    "typecheck": "bunx tsc --noEmit",
    "check": "bun test tests/ && bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "openai": "^6.39.0"
  }
}
